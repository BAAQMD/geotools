.onLoad <- function(libname, pkgname) {

  # st_sample_msg <-
  #   stringr::str_c(
  #     "-------------------------------------------------------------------------------",
  #     "A better `st_sample()` is now provided by the `sf` package.",
  #     "Its behavior is different, but more well-supported and future-proofed. ",
  #     "If you really want to use the old `geotools::st_sample()`, ",
  #     "then please invoke `st_sample_geotools()` instead.",
  #     "-------------------------------------------------------------------------------",
  #     sep = "\n")
  #
  # packageStartupMessage(
  #   st_sample_msg)

}
